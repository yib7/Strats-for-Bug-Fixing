# GENERATED by scripts/gen_scaling_configs.py -- do not edit by hand; edit the generator.
# Data-scaling curve, arm B (no pretraining, randomly initialized): train_n=15000, seed=1.
# The 52K (full-data) point is NOT generated -- it reuses the existing finetune_B_seed{0,1} runs.

seed: 1
tokenizer_path: outputs/tokenizer/tokenizer.model

train_split: train
validation_split: validation
max_seq_length: 512

train_n: 15000

epochs: 10
# effective batch 64 = 8 x 8 (see pretrain_10ep.yaml); 64 per-device OOMs 16 GB at seq 512
batch_size: 8
gradient_accumulation_steps: 8
lr: 5.0e-5
warmup_steps: 500

output_dir: outputs/finetune_scale_B_n15k_seed1

model:
  d_model: 512
  d_ff: 2048
  d_kv: 64
  num_heads: 8
  num_layers: 6
  num_decoder_layers: 6
