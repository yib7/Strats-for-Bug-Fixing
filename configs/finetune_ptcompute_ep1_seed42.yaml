# GENERATED by scripts/gen_scaling_configs.py -- do not edit by hand; edit the generator.
# Pretrain-compute curve, arm A: pretrained for 1 epoch(s), then 10 finetune epochs, full data.
# pretrained_model_path points at the stable outputs/pretrain/epoch-1 dir written by
# pop.train.pretrain's milestone callback. The ep10 point is NOT generated -- pretrain-final
# == epoch-10, so it reuses the existing finetune_A_ep10 run.

seed: 42
tokenizer_path: outputs/tokenizer/tokenizer.model
pretrained_model_path: outputs/pretrain/epoch-1

train_split: train
validation_split: validation
max_seq_length: 512

epochs: 10
# effective batch 64 = 8 x 8 (see pretrain_10ep.yaml); 64 per-device OOMs 16 GB at seq 512
batch_size: 8
gradient_accumulation_steps: 8
lr: 5.0e-5
warmup_steps: 500

output_dir: outputs/finetune_ptcompute_ep1_seed42

model:
  d_model: 512
  d_ff: 2048
  d_kv: 64
  num_heads: 8
  num_layers: 6
  num_decoder_layers: 6
